# DEPRECATED: This workflow is no longer needed after migration to INNO Setup
# INNO Setup automatically reads version from src/ofxEmotiBitVersion.h
# Old workflow checked if .vdproj ProductVersion matched ofxEmotiBitVersion.h
# Kept for reference only - will be deleted in future cleanup

name: check Installer project version (DEPRECATED)
on:
  # Disabled - no longer needed with INNO Setup
  # pull_request:
  #   types: [opened, reopened, synchronized]
  #   branches:
  #     - dev
  workflow_dispatch:

jobs:
  checkout:
    runs-on: [self-hosted, Linux]
    environment: RUNNER-LINUX
    steps:
      - name: checkout repository
        uses: actions/checkout@v4  
  check-installer-project-version:
    needs: checkout
    runs-on: [self-hosted, Linux]
    steps:
      - name: check-version
        run: |
          pwd
          cd tests/EmotiBitInstaller/check-installer-version
          bash run_test.sh
